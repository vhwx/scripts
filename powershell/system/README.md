# Get-DiskUsageReport.ps1

Reports disk usage for a directory, sorted largest first.

## Quickstart

```
./Get-DiskUsageReport.ps1 -Path "C:\Projects"
./Get-DiskUsageReport.ps1        # defaults to current directory
```

Requires PowerShell 7+ (`pwsh`).
