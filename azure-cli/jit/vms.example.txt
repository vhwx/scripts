# Sample batch input for az-jit-request.sh --input-file
#
# One VM name per line, scoped to the --subscription/--resource-group given on the
# command line. Blank lines and lines starting with # are ignored.
#
# Example usage:
#   ./az-jit-request.sh --subscription <subscription-id> --resource-group example-rg \
#       --input-file vms.example.txt --ports 22,3389 --dry-run
#
#   ./az-jit-request.sh --subscription <subscription-id> --resource-group example-rg \
#       --input-file vms.example.txt --configure \
#       --ip-ranges 203.0.113.0/24,198.51.100.10/32

web-01
web-02
app-vm-01
