# Quickstart

Add the target virtual machines in the csv file and run:

`./az-jit-request.sh --input-file vms.csv --configure-and-request`

Alternatively after configure is done once:

`./az-jit-request.sh --input-file vms.csv`
